## 디렉토리 개요

워커 프로세스 엔트리포인트를 둔다. 오케스트레이터가 이 엔트리포인트를 spawn 하여 실제 Slack/스케줄러 작업을 수행한다.

## 코드 작성 컨벤션

- 워커는 외부 포트를 직접 소유하지 않고, 오케스트레이터가 지정한 내부 루프백 포트(`PORT`)로만 listen 한다.
- `--mcp-server` 분기는 워커에서 처리한다.
- 종료 시그널(`SIGINT`, `SIGTERM`)을 받으면 런타임을 정리한 뒤 종료한다.
